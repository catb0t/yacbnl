sudo: true
language: c
install:
  - "yes | sudo add-apt-repository ppa:snaipewastaken/ppa"
  - sudo apt-get update
  - sudo apt-get install criterion

script:
  - util/premake-linux64 gmake
  - make -j
  - bin/test_yacbnl
